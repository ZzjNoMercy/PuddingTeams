# 周会要点 · 2026-08-06

## 1. 阶段二「房间即群聊」主体落地（本周完成）

- **三种窗口模型**（solo 置顶单例 / 单聊 / 群聊）按方案文档落地，窗口成为侧栏一级实体，session 归属窗口内管理。
- **solo 派活路由**：`team_task` 动态白名单 + `session: new/continue` 续接决策；派活结果自动同步写入对应单聊窗口（`pudding:task_assign` / `pudding:task_result`，best-effort，失败降级不阻塞派活）。
- **成员消息流**：worker 以独立身份（头像+名字）出现在时间线；头像体系完成（上传/删除 + 程序化默认头像，全站组件一处升级处处生效）。
- 对照方案文档逐项核对，§1–§7、§11 均已勾销；本轮累计约 2270 行改动，**待 review 后提交**。

## 2. 遗留问题与风险（需关注）

- **进程管理**：tsx watch 下 WS 连接导致 SIGTERM 后进程不退，重启需 kill -9 —— 后续换显式 supervisor 或修 WS 清理。
- **SDK 升级回归点两处**：① `sendCustomMessage` 无 trigger 分支（agent-session.ts:1451-1461）；② 同步前的 force-flush hack（`SessionManager.flushed` 私有字段）。升级前必须回归验证。
- **数据清理**：`.teams/` 含旧 bug 产生的多余 session 与测试消息，未清理。

## 3. 下周计划

- **`workers/<name>/` 共享工作目录**（方案 §8.3，唯一未开工的较大项）。
- **收尾**：提交本轮改动、清理脏数据、跑一遍端到端验收（含 SDK 升级回归点）。
- 明确阶段二完成标准与演示/验收节点，同步给相关方。
